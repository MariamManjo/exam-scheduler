{
  "name": "exam-scheduler",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "build": "npm run typecheck:api && npm run build --prefix frontend",
    "typecheck:api": "tsc -p api/tsconfig.json --noEmit",
    "lint": "npm run lint --prefix frontend"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "openai": "^6.44.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "@vercel/node": "^5.3.21",
    "typescript": "~6.0.2",
    "vercel": "^41.7.0"
  }
}
