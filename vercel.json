{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "npm install && npm install --prefix frontend && npm run build:api",
  "buildCommand": "npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/extract.js": {
      "maxDuration": 60,
      "memory": 1024
    },
    "api/calculate.js": {
      "maxDuration": 60,
      "memory": 1024
    }
  }
}
